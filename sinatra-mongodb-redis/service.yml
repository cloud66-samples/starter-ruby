# Generated by Cloud66 Starter
---
services:

  worker:
    git: git@github.com:cloud66-samples/starter-ruby.git
    git_branch: master
    command: bundle exec rake resque:work QUEUE=*
    build_root: sinatra-mongodb-redis/
    env_vars:
      RAILS_ENV: production
      RACK_ENV: production

  web:
    git: git@github.com:cloud66-samples/starter-ruby.git
    git_branch: master
    command: bundle exec rackup -E _env:RACK_ENV
    build_root: sinatra-mongodb-redis/
    ports:
      - container: 9292
        http: 80
        https: 443
    env_vars:
      RAILS_ENV: production
      RACK_ENV: production

databases:
  - postgresql
  - redis
  - mongodb
